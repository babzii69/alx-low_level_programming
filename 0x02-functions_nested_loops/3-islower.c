#include "main.h"

/**
  * _islower - A function that checks for lowercase character
  *
  * @c: The parameter is to be printed
  *
  * Return: 1 if it is lowercase character or 0 for anything else
  */
int _islower(int c)
{
	if (c >= 97 && c <= 122)

	{	return (1);

	}	return (0);
}

